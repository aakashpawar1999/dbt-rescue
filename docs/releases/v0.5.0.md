# dbt-rescue 0.5.0

## Planned delivery

- Default the focused public walkthrough to the fictional Sunita case
  `DBT-SUNITA-001`.
- Use relative Vite assets so the same static artifact can run locally or under
  a repository-hosted path.
- Link the in-app simulation disclosure to the functional boundary and known
  limitations.
- Add GitHub Pages build/deploy automation and a timed three-minute demo script,
  transcript, submission write-up, and manifest.
- Keep all records, sources, acknowledgements, retries, reissues, and credits
  fictional or simulated.

## Verification recorded so far

- `bun run test` — 31 tests passed, 0 failures.
- `bun run lint` — TypeScript completed with exit 0.
- `bun run build` — Vite production build completed with exit 0.
- `git diff --check` — exit 0.

## Remaining release gates

- Push and merge the approved release branch through the repository's workflow.
- Enable/verify GitHub Pages and check the intended URL in a signed-out browser.
- Create and verify tag `v0.5.0` and its GitHub Release.
- Publish and verify the timed transcript for the actual release build.
- Fill the submission manifest with the final commit, artifact date, URLs, and
  organiser destination.

## Boundary

This remains an independent hackathon prototype. It does not connect to live
government, PFMS, NPCI, Aadhaar, bank, identity, analytics, model, grievance,
or payment systems; it does not determine eligibility, correct real records,
release money, or confirm a real credit.
